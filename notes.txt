CGI :

- mettre des repères pour la boucle POLLOUT pour retomber dans la bonne fonction qui va lire/écrire

1 - Is CGI ? (on ne fonctionne que avec l extension ? pas de dossier binaire CGI ?)
	-> analyse de la request line
		-> repérer le premier fichier (et non dossier) qui match avec l extension
			-> essayer de garder en mémoire l index dans string pour retrouver les path info, etc..
		-> le reste = PATH_INFO si '/' / QUERY si '?'
		-> fin = # ou fin de la string !!! à voir comment c'est traité actuellement l'analyse de la string
2 - Methode ?
	-> GET : 
		-> paramétrer les variables d'env en fonction de l'env/requête // DONE
		-> pipe / fork
			-> dans le père, fermant la partie entrante, puis lire et envoyer au fur et à mesure la partie sortante
			-> dans le fils, fermer la partie sortante, et dup2 stdout avec la partie entrante
		-> la boucle pollout revient lire et envoyer ce qui est sur la partie sortante du FD
	-> POST :
		-> paramétrer les variables d'env en fonction de l'env/requête // DONE
		-> faire 2 pipes + fork
			-> un premier pipe pour écrire depuis le père dans le fils le contenu du body
			-> un deuxième pour lire stdout du fils / lire / send (comme dans un get)



---

A voir avec Guillaume :
	- comment est paramétrer PATH_INFO (nom de la variable dans config, valeur si pas de PATH, présence de / au début, etc..)
	- idem pour PATH_TRANSLATED
	- idem pour QUERY_STRING
	- pareil pour script name